# INSTALL.md — Serotonin 범용 OBD 설치 안내

> 이 문서는 앱을 받는 사람에게 그대로 전달할 수 있게 쓴 것입니다.
> 제작자: 이은호 (serotonin.1207@gmail.com)

---

## 1. 이 앱이 무엇인가

자동차의 **OBD-II 진단 포트**에 꽂는 블루투스 어댑터로 차량의 오류코드를 읽는 앱입니다.
일반 차량은 물론, 표준 OBD가 통하지 않는 전기차의 ECU 도 주소를 직접 찾아 읽습니다.

**할 수 있는 것**
- 표준 오류코드 읽기 (저장·보류·영구)
- 경고등 점등 여부와 코드 개수 확인
- 고장 발생 순간의 데이터(프리즈 프레임) 저장
- **표준 OBD가 안 되는 차량은 ECU 를 직접 찾아 오류코드 읽기 (UDS)** — 전기차 포함
- ECU 데이터 스캔 (부품번호·소프트웨어 버전 등)
- 진단 결과를 파일로 저장·공유, 정비소 전달용 복사
- 오류코드 삭제 (표준 / ECU 각각, 조건을 모두 만족했을 때만, 1회)

**할 수 없는 것**
- ABS, 에어백, TPMS 등 **안전 계통 제어** — 읽기만 하고 작동시키지 않습니다
- 접촉기 강제 작동, 충전 강제 시작 — 차량 보호 장치를 무력화하는 명령은 넣지 않았습니다
- 전기차 배터리 셀 전압 등 검증되지 않은 제조사 전용 데이터 (추측해서 보내지 않습니다)

> ⚠️ 이 앱은 개인 진단 보조 도구이며 정비소의 전문 진단을 대체하지 않습니다.
> 오류코드 삭제는 고장을 수리하지 않습니다.
> 고전압 배터리, 절연, 충전기, 브레이크, 에어백 관련 이상이 있으면
> 차량을 운행하거나 충전하지 말고 전문 점검을 받으십시오.

---

## 2. 필요한 것

| 항목 | 조건 |
|---|---|
| 스마트폰 | **Android 8.0 이상** (그 아래는 설치되지 않습니다) |
| 블루투스 | Classic 지원 — 사실상 모든 안드로이드 폰 |
| 어댑터 | ELM327 계열 **Bluetooth 3.0 (Classic)** 제품 |
| 차량 | OBD-II 규격을 따르는 차량 |

### ⚠️ 어댑터 고르실 때

**"BLE" · "Bluetooth 4.0" · "iOS 전용" 제품은 이 앱과 호환되지 않습니다.**
반드시 **BT3.0 / Bluetooth Classic / 안드로이드용** 이라고 적힌 제품을 고르십시오.

검증된 제품: Vgate iCar Pro ELM327 **BT3.0**

---

## 3. 설치 방법

### 3-1. APK 파일 받기

전달받은 `app-release.apk` 파일을 폰에 저장합니다. (카카오톡, 메일, USB 등 어느 방법이든 무방)

### 3-2. "출처를 알 수 없는 앱" 허용

Play 스토어를 거치지 않은 앱이라 한 번 허용해 주어야 합니다.

1. 파일을 열어 설치를 시도합니다
2. **"이 출처의 앱을 설치할 수 없습니다"** 안내가 뜹니다
3. **설정** 을 누릅니다
4. **이 출처 허용** 을 켭니다
5. 뒤로 가서 다시 설치합니다

> 이 허용은 **파일을 여는 앱별로** 설정됩니다.
> 카카오톡으로 받았으면 카카오톡에, 파일 앱으로 열었으면 파일 앱에 허용이 필요합니다.

### 3-3. Play Protect 경고

설치 중 **"알 수 없는 앱입니다"** 또는 **"스캔되지 않은 앱"** 경고가 뜰 수 있습니다.
Play 스토어에 등록되지 않은 앱이라 나오는 정상적인 안내입니다.
**설치** 또는 **무시하고 설치** 를 누르면 됩니다.

### 3-4. 권한 허용

앱을 처음 실행하고 **어댑터 연결** 화면에 들어가면 **'근처 기기'** 권한을 묻습니다.
어댑터와 통신하는 데 필요합니다. **허용** 을 눌러 주십시오.

- 이 앱은 **위치정보를 사용하지 않습니다.**
- 인터넷은 앱 버전 확인과 사용자가 누른 서명된 공개 오류코드 데이터 갱신에만 사용합니다. 진단 데이터는 전송하지 않으며 설정에서 업데이트 확인을 끌 수 있습니다.

---

## 4. 처음 사용하기

### 4-1. 어댑터 없이 먼저 둘러보기

설정 → **테스트 모드** 를 켜면 어댑터 없이 앱 사용법을 익힐 수 있습니다.
이 모드에서 나오는 코드는 **전부 가짜**이며, 화면 위쪽에 "모의 데이터" 표시가 계속 뜹니다.

실제로 진단할 때는 **반드시 테스트 모드를 꺼야** 합니다.

### 4-2. 내 차량 이름 설정

설정 → **진단 대상 차량** 에서 차량 이름을 바꿔 두면 저장되는 기록에도 그대로 남습니다.

### 4-3. 실제 진단

1. 차량을 안전한 곳에 주차하고, 변속을 **P**, 주차 브레이크를 채웁니다
2. **충전 케이블(전기차)을 분리**합니다
3. 어댑터를 운전석 아래 OBD-II 포트에 꽂습니다 (어댑터 LED 점등 확인)
4. 폰 **설정 → 연결 → Bluetooth** 에서 어댑터를 **페어링**합니다
   - 이름 예시: `V-LINK`, `OBDII`, `Vgate`, `iCar Pro`
   - PIN을 물으면 판매자 설명을 확인하십시오 (보통 `1234` 또는 `0000`)
5. 차량 전원을 켭니다 (전기차는 브레이크를 밟고 전원 버튼 — 전원이 꺼져 있으면 일부 ECU가 응답하지 않습니다)
6. Serotonin OBD 앱 → **어댑터 연결** → 목록에서 선택 → **연결**
7. **진단 시작** → **진단 실행** (아래가 자동으로 진행됩니다)
   - 초기화 → 프로토콜 자동 탐색 → 표준 OBD 읽기
   - 표준으로 코드가 없으면 **ECU 를 직접 찾아 조회** (전기차)
8. 결과가 자동으로 파일에 저장됩니다
9. **진단 기록** 에서 파일 공유, **ECU 오류코드** 화면에서 결과 복사

---

## 5. 오류코드 삭제에 대하여

삭제 화면이 **두 가지**입니다. 차량에 맞는 쪽을 쓰십시오.

| 화면 | 대상 |
|---|---|
| **오류코드 삭제** | 표준 OBD(Mode 04). 일반 휘발유·경유 차량 |
| **ECU 오류코드 삭제** | ECU 주소로 직접 삭제(UDS). 전기차처럼 표준 OBD가 안 되는 차량 |

잘못된 화면을 열면 앱이 경고하고 맞는 화면으로 안내합니다.

삭제 기능은 **일부러 까다롭게** 만들어져 있습니다. 다음을 **모두** 만족해야 버튼이 켜집니다.

1. 어댑터 연결 · 진단 실행 (코드 읽기)
2. 결과가 파일로 저장됨
3. 안전 확인 항목 모두 선택
4. 확인 문구 `오류코드를 저장했습니다` 직접 입력

삭제는 **1회 실행**되고, 3초 후 자동으로 다시 읽어 전후를 비교합니다.

### 삭제하기 전에 알아야 할 것

- **삭제는 고장을 고치지 않습니다.** 원인이 남아 있으면 코드는 다시 뜹니다.
- 지운 직후 화면에 **"지금도 고장을 감지하고 있습니다"** 가 나오면, ECU 가 그 고장을
  실시간으로 보고 있다는 뜻입니다. **반복해서 지워도 결과는 같습니다.**
- 특히 고전압 배터리 계통 코드가 이 상태라면, 차량이 충전·주행을 막는 것은 보호 동작입니다.
  기록을 지워 잠시 동작하더라도 원인이 해결된 것은 아닙니다.
- 삭제하면 표준 삭제의 경우 프리즈 프레임(고장 순간 데이터)도 사라집니다.
  앱이 삭제 전에 저장해 두지만, 차량에서는 없어집니다.

### 이럴 때는 즉시 멈추고 정비소로

타는 냄새 · 연기 · 비정상 발열 · 고전압 경고 · 절연 오류 · 배터리 누액 의심 ·
브레이크나 에어백 경고 · **삭제 직후 코드가 바로 재발** · 차량이 시동(READY) 상태로 안 들어감

---

## 6. 개인정보

- 진단 기록은 **폰 안에만** 저장됩니다
- 앱에는 진단 데이터를 전송하는 기능이 없으며, 업데이트 GET 요청에 차량·진단 정보를 포함하지 않습니다
- 블루투스 주소는 마지막 2바이트만 저장됩니다
- 차대번호(VIN)는 기본적으로 읽지 않으며, 켜더라도 파일에는 가려서 저장됩니다
- 광고·분석 도구가 없습니다

기록을 남에게 보낼 때는 **공유 버튼**을 눌렀을 때만 나갑니다.

---

## 7. 잘 안 될 때

| 증상 | 확인할 것 |
|---|---|
| 설치가 안 됨 | Android 8.0 이상인지, "출처를 알 수 없는 앱" 허용했는지 |
| 어댑터가 목록에 없음 | 폰 **시스템** Bluetooth 설정에서 먼저 페어링했는지 |
| 연결 실패 | 어댑터 LED 점등, 다른 앱(다른 OBD 앱)이 어댑터를 쓰고 있지 않은지 |
| 연결은 되는데 코드가 안 읽힘 | 차량 전원이 켜져 있는지(ACC 또는 READY) |
| `UNABLE TO CONNECT` 만 나옴 | ① 차량을 **READY(시동)** 상태로 두고 다시 시도 ② 설정 → **연결 프로토콜** 에서 `6. ISO 15765-4 CAN 11bit/500k` 로 고정 후 재시도 |
| `NO DATA` 만 나옴 | 오류가 없거나 차량이 그 모드를 지원하지 않음 — 앱 문제가 아닙니다 |
| 전기차인데 표준 코드가 안 나옴 | 정상입니다. 진단 실행 시 앱이 자동으로 ECU 를 직접 찾아 조회합니다 |
| ECU 스캔에서 응답이 적음 | 차량 전원을 켜세요. 꺼지면 모듈이 절전에 들어가 응답이 줄어듭니다 |

---

## 8. 면책

이 소프트웨어는 있는 그대로 제공되며, 사용으로 발생하는 차량 손상·데이터 손실·인적 피해에 대해
제작자는 책임지지 않습니다. 차량 진단과 수리의 최종 판단과 책임은 사용자에게 있습니다.
안전 관련 이상이 의심되면 반드시 공식 서비스센터 또는 자격을 갖춘 정비사의 점검을 받으십시오.
