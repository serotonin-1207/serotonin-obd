# TESTING.md — LeafOBD 테스트 안내

## 0. 환경

| 항목 | 값 |
|---|---|
| JDK | Android Studio 내장 JBR (OpenJDK 21) |
| Gradle | 9.5.0 (wrapper) |
| AGP | 9.3.1 |
| Kotlin | 2.2.10 |
| compileSdk / targetSdk | 37 / 36 |
| minSdk | 26 |
| 테스트 기기 | 삼성 갤럭시 A23 (SM-A235N, Android 14 / API 34) |

명령줄에서 실행할 때는 JAVA_HOME을 먼저 지정한다.

```bash
export JAVA_HOME="/c/Program Files/Android/Android Studio/jbr"
```

Windows PowerShell:

```bash
$env:JAVA_HOME="C:\Program Files\Android\Android Studio\jbr"
```

## 1. 단위 테스트

```bash
./gradlew.bat testDebugUnitTest
```

리포트: `app/build/reports/tests/testDebugUnitTest/index.html`

| 테스트 파일 | 검증 대상 |
|---|---|
| `obd/DtcParserTest.kt` | 2바이트 → 표준 코드 변환, 단일/다중 코드, 개수 바이트 유무, 0000 패딩, echo, 공백, CRLF, 다중 프레임, 빈 응답, NO DATA, `?`, CAN ERROR, 잘린 응답, 홀수 길이, Mode 03/07/0A 구분 |
| `obd/VoltageParserTest.kt` | ATRV 정상/echo/공백/해석 실패, 저전압 안내 |
| `util/ResponseTextTest.kt` | 프롬프트·잡음 제거, 오류 키워드 한국어 변환, 16진 변환 |
| `obd/ObdFrameParserTest.kt` | ISO-TP 재조립(단일·최초·연속 프레임), 11/29비트 헤더, 다중 ECU 분리 |
| `obd/VehicleInfoParserTest.kt` | 지원 PID 비트맵, 0101 경고등·DTC 개수, PID 계산식, 프리즈 프레임 원인 DTC, VIN 파싱·마스킹 |
| `elm327/Elm327ClientTest.kt` | 초기화 시퀀스 순서, 타임아웃, NO DATA, CAN ERROR, 삭제 후 재조회, 영구 코드 잔존, 지원 PID 연속 조회, 프리즈 프레임 소실 재현 |
| `viewmodel/ClearEligibilityTest.kt` | 삭제 사전조건 6가지, 확인 문구 일치 |
| `log/LogFormatTest.kt` | TXT/JSON 형식, MAC 마스킹, 파일명 규칙 |

## 2. 테스트 모드 (어댑터 없이)

설정 화면 → **테스트 모드** 켜기 → 시나리오 선택.

| 시나리오 | 확인 내용 |
|---|---|
| 표준 응답 | 정상 흐름, 코드 1건, 삭제 후 모두 사라짐 |
| 삭제 후 재발 | 코드 4건 + 영구 1건, 삭제해도 P0AA6 재발 → 재발 강조와 경고 표시 확인 |
| 데이터 없음 | `NO DATA` 가 오류가 아니라 "데이터 없음"으로 표시되는지 |
| 통신 오류 | `CAN ERROR` 한국어 안내 표시 |
| 무응답 | 타임아웃 메시지 표시, 앱이 멈추거나 죽지 않는지 |
| 자동 프로토콜 실패 | 자동 선택 실패 후 순차 시도로 프로토콜 6을 찾아내는지, "고정하면 빨라집니다" 안내가 뜨는지 |

테스트 모드에서는 모든 화면 상단에 **"모의 데이터"** 배지가 표시되어야 한다.
배지가 없으면 버그다.

## 3. 실제 기기 테스트 (어댑터 없이)

갤럭시 A23(SM-A235N), Android 14 / API 34 기준.

> 2026-07-30 확인 완료: adb 로 화면을 조작하며 전 기능 검증.
> 아래 체크박스 중 미표시 항목은 아직 확인하지 않은 것이다.

- [x] 개발자 옵션 → USB 디버깅 켜기
- [x] `adb devices` 에서 기기가 보임 (처음에는 `unauthorized` → 폰에서 허용 필요)
- [x] APK 설치 성공
- [x] 앱 실행, 홈 화면 정상 표시
- [x] 테스트 모드 진단 **10단계** 전부 성공
- [x] 프리즈 프레임 원인 DTC `P0AA6` 표시
- [x] 경고등 점등 · ECU 보고 코드 4건 · 지원 PID 7개 · 실시간 값 4종
- [x] VIN 화면 전체 표시 / 파일 마스킹 저장 (전체 VIN 미포함을 grep 으로 확인)
- [x] 삭제 조건 4충족 2미충족 → 삭제 버튼 잠김
- [x] 체크박스 3개 선택 시 조건 5충족으로 변경
- [x] 진단 기록 목록 및 TXT·JSON 공유 버튼 표시
- [x] 설정이 앱 재시작 후에도 유지 (SharedPreferences)
- [x] **무응답 시나리오** — 타임아웃 후에도 멈춤·크래시 없음, 각 단계 실패로 표시
- [ ] 확인 문구 입력 후 실제 삭제 실행 — adb 로 한글 입력이 불가해 직접 확인 필요
- [ ] 표준 응답 / 데이터 없음 / 통신 오류 시나리오
- [ ] 권한 **거부** 시 크래시 없이 안내
- [ ] Bluetooth 꺼짐 상태 안내
- [ ] 화면 회전 · 백그라운드 복귀 후 상태 유지
- [ ] 첫 실행 시 Bluetooth 권한 요청 대화상자가 뜸 (Android 12+ 이므로 `BLUETOOTH_CONNECT`)
- [ ] 권한 **거부** 시 앱이 죽지 않고 안내 화면 + 설정 이동 버튼 표시
- [ ] 설정 이동 버튼이 앱 정보 화면을 엶
- [ ] 권한 허용 후 페어링된 장치 목록 표시
- [ ] Bluetooth **끈 상태**에서 안내 메시지 표시, 크래시 없음
- [ ] 장치가 하나도 없을 때 "시스템 설정에서 먼저 페어링" 안내 표시
- [ ] 홈 → 장치 → 진단 → 삭제 → 로그 → 설정 화면 이동 전부 정상
- [ ] 화면 회전 후에도 상태 유지
- [ ] 앱을 백그라운드로 보냈다가 복귀해도 크래시 없음
- [ ] 안전 경고 카드가 홈 화면과 삭제 화면에 표시됨

## 4. 실제 어댑터 테스트 (차량 없이)

어댑터에 12V를 공급할 수 있는 경우에만 가능하다. 대부분은 차량에 꽂아야 한다.

- [ ] 시스템 Bluetooth 설정에서 어댑터 페어링 (`V-LINK` / `OBDII` / `Vgate` / `iCar Pro` 등)
- [ ] 앱 장치 목록에 표시됨, 주소가 마스킹되어 있음
- [ ] 연결 시도 → 성공/실패 메시지가 한국어로 표시
- [ ] `ATI` 응답 표시
- [ ] `ATRV` 응답 표시
- [ ] 연결 해제 후 재연결 가능
- [ ] 어댑터를 뽑았을 때 오류 메시지 표시, 크래시 없음

## 5. 실제 차량 테스트 체크리스트

> **작업 전 [SAFETY.md](SAFETY.md) 를 반드시 먼저 읽는다.**

### 준비

- [ ] 안전한 장소에 주차
- [ ] 변속 위치 P
- [ ] 주차 브레이크 체결
- [ ] 충전 케이블 분리
- [ ] 고전압 부품(주황색 배선) 접촉 금지
- [ ] OBD2 어댑터를 운전석 하단 포트에 결합
- [ ] 스마트폰에서 어댑터 페어링 완료

### 읽기

- [ ] 앱 실행 → 어댑터 연결
- [ ] ELM327 초기화 8단계 모두 성공
- [ ] `ATI` 결과 기록
- [ ] `ATRV` 결과 기록
- [ ] `0101` 경고등 상태와 ECU 보고 DTC 개수 기록 (Mode 03 결과와 비교)
- [ ] 지원 PID 개수 기록
- [ ] **프리즈 프레임 결과 기록** — 원인 DTC가 있으면 반드시 저장 (삭제하면 사라진다)
- [ ] Mode 03 결과 기록
- [ ] Mode 07 결과 기록
- [ ] Mode 0A 결과 기록
- [ ] (선택) 설정에서 CAN 헤더 표시를 켜고 다시 읽어 어느 ECU가 응답하는지 확인
- [ ] (선택) 설정에서 VIN 읽기를 켜고 통신이 정상인지 확인
- [ ] 원시 응답 펼쳐서 화면 캡처
- [ ] TXT 로그 저장
- [ ] JSON 로그 저장
- [ ] 로그를 다른 곳(메일·메모)으로 공유해 백업

### 삭제 (선택 사항, 로그 저장 후에만)

- [ ] 저장된 로그 파일이 실제로 존재하는지 확인
- [ ] 체크박스 3개 선택
- [ ] 확인 문구 `오류코드를 저장했습니다` 입력
- [ ] 삭제 1회 실행
- [ ] 결과 자동 저장 확인
- [ ] 삭제 전후 비교 화면 확인
- [ ] 차량 전원 완전히 끄기
- [ ] 스마트키를 차량에서 멀리 두기
- [ ] 수 분 후 차량 다시 켜기
- [ ] 오류코드 재조회
- [ ] 고전압 관련 코드 재발 여부 확인 → **재발 시 충전·운행 중단하고 전문 점검**

### 중단 조건

타는 냄새, 연기, 비정상 발열, 고전압 경고, 절연 오류, 배터리 누액 의심,
브레이크·에어백 경고, 삭제 직후 즉시 재발, READY 미진입 → **즉시 중단.**

## 6. 알려진 제약

- 프로젝트 경로에 한글과 공백이 있어 `android.overridePathCheck=true` 를 사용한다.
  빌드 도구에서 경로 관련 오류가 발생하면 프로젝트를 ASCII 경로(예: `C:\python\LeafOBD`)로 옮긴다.
- Mode 03 응답에서 "개수 바이트가 있는 CAN 형식"과 "개수 바이트가 없는 KWP 형식"은
  길이 검사 휴리스틱으로 구분한다. 드물게 오판할 수 있으므로 **원시 응답을 항상 함께 확인한다.**
- Nissan Leaf EV 전용 오류는 표준 OBD-II로 읽히지 않을 수 있다. 앱의 결함이 아니다.
