## 2026-09-14 — Serotonin 범용 OBD 2.0

- 앱 버전을 `2.0`(`versionCode 3`)으로 올리고 차량·진단 파형을 형상화한 새 범용 아이콘을 적용했다.
- 차량 프로필, 차종별 지원 범위, 105개 오류코드 해설, 미해설 코드 대기함과 서명된 공개 데이터 업데이트를 한 흐름으로 정리했다.
- Nissan Leaf ZE1·Kia Niro EV DE의 SOC·BMS 보고 SOH·셀 전압·온도 읽기 시험과 그래프, 용어 설명, 배터리 PDF 보고서를 추가했다. 직접 읽은 수치는 기준 진단기와 대조하기 전까지 실차 검증 필요로 표시한다.
- 페어링된 장치마다 `이 장치 연결` 버튼을 제공하고 OBD 추정 장치를 먼저 보여 주어 처음 연결할 때 선택과 연결 단계를 구분하기 쉽게 바꿨다.
- 일반 사용자 보기와 정비사 보기를 분리하고, 진단 기록·원시 응답·정비 전후 PDF 보고서를 추가했다.
- 표준 Mode 04와 제한된 UDS 삭제는 삭제 전 기록, 정차 조건, 이중 확인, 세션당 1회, 삭제 후 자동 재조회 원칙을 유지한다. 서비스센터 안내에 따라 삭제하는 경우의 후속 확인 절차를 별도로 설명한다.
- 공개 배포용 release APK와 실제 Galaxy A23 화면을 검증했다.
- 검증: 단위 테스트 299건 실패/오류 0, Galaxy A23 기기 테스트 8건, debug/release APK와 release Lint 통과.

## 2026-09-13 — P0A0D 고전압 인터록 안전 해설

- P0A0D를 SAE J2012 표준 체계와 현대 YF·LF, 기아 Niro·Optima 계열 공개 문서에서 대조하고 공통 OBD 해설에 추가했다.
- `High`를 배터리 과전압으로 오해하지 않도록 설명하고, READY 불가·고전압 경고·충돌·침수 시 운행과 충전을 중단하도록 안내한다.
- 사용자가 주황색 케이블·고전압 커넥터·서비스 플러그를 만지지 않게 하고, ECU·세부 코드·정비 이력과 제조사 고전압 차단 절차를 정비사 전달 항목으로 분리했다.
- SAE J2012 Digital Annex는 유료 저작물이므로 원문 코드를 복제하지 않고 계약 필요 상태를 출처 원장에 기록했다.
- 지식 팩을 105건(공통 102건, 제조사 전용 3건), 근거 34개, `2026.09.13-public.7`, 서명 리비전 7로 올렸다.
- 검증: 단위 테스트 299건 실패/오류 0, Galaxy A23 기기 테스트 8건, debug/release APK와 release Lint 통과.

## 2026-09-13 — 교차 검증 공통 코드 4개 해설 승격

- P00B7·P2118·P0236·P0299를 공통 OBD 해설에 추가해 지식 팩을 104건(공통 101건, 제조사 전용 3건)과 공식 근거 문서 32개로 확대했다.
- 각 코드에 조건부 운행 안내, 사용자가 확인할 항목, 정비사 전달 항목과 삭제 전 보존 항목을 넣었다.
- 제조사 공지는 코드 의미의 교차 검증 근거로 사용하고, 해당 문서의 원인·임계값·수리 절차를 다른 차종에 일반화하지 않는다.
- 지식 팩 버전을 `2026.09.13-public.6`, 서명 리비전을 6으로 올렸다.
- 검증: 단위 테스트 298건 실패/오류 0, Galaxy A23 기기 테스트 8건, debug/release APK와 release Lint 통과.

## 2026-09-13 — 범용 지식 팩 1차 확대와 미해설 코드 대기함

- 등록한 차량의 기능별 상태를 `지원`, `부분 지원`, `실차 검증 필요`, `미지원`으로 보여주는 `내 차 지원 범위` 화면을 추가했다.
- 표준 OBD, 제조사 코드 읽기·삭제, EV 배터리, ABS·에어백, 차체, 액추에이터·코딩, 로컬 보고서를 각각 판정하고 근거와 제한을 함께 표시한다.
- Leaf ZE1·Niro EV DE 배터리는 구현 여부와 실차 정확도 검증을 분리해 기준 진단기 대조 전까지 `실차 검증 필요`로 표시한다.
- 차량 카탈로그 리비전 1을 추가해 사용자 확인 2019 리프 ZE1·니로 EV DE, 현대·기아 내연기관 검증 대상군, 범용 내연기관·전기차·기타 차량을 구분한다.
- 다른 연식이나 이름만 비슷한 차량을 사용자 확인 차량으로 확대하지 않으며, 차량 추가 화면에 안전한 빠른 입력을 제공한다.
- 차량 프로필에 판매 지역, 엔진·배터리 사양, 변속기 필드를 추가했다. 기존 프로필에는 `모름`과 빈 선택값을 적용해 계속 읽는다.
- 북미 공식 공개 공지의 적용 범위가 명확한 2011–2015 Sonata Hybrid YF, 2015 Soul PS 1.6L, 2008 Rio/Rio5 JB 1.6L, 2017 IONIQ Hybrid를 카탈로그에 추가했다.
- 판매 지역·연식·세대·동력계·엔진 조건이 모두 맞을 때만 공식 문서 항목에 연결하며, 앱에서 NHTSA 공개 원문을 직접 열 수 있다.
- 대한민국 현대 15개·기아 21개 모델 계열을 각각의 검증 대기 ID로 추가했다. 한글·대표 해외 차명 별칭을 함께 인식한다.
- 차량 추가 화면에 스크롤 가능한 국내 주요 차종 선택 목록을 넣고, 선택 후 연식·세대·동력계를 사용자가 확인하도록 저장 전 입력을 유지한다.
- 새 진단 JSON을 `reportSchema 2`로 올리고 진단 당시 프로필 UUID, 차량 계열 ID·표시명, 카탈로그 리비전, 제조사·차종·연식·동력계·판매지역·사양을 스냅샷으로 저장한다.
- 미해설 코드 대기함에 모델 계열별 차량 수·후보 수·코드 수신 세션 합계와 계열 필터를 추가했다. 기존 기록은 현재 프로필을 대입하지 않고 계열 미저장으로 남긴다.
- 익명 검증 후보에는 저장 당시 차량 계열만 포함하고 차량 별칭과 현재 프로필 사양을 대입하지 않는다.
- 새 기록에 프로토콜 식별 여부, 표준 OBD 데이터 실제 관찰 여부, ECU 스캔 응답 수를 별도 저장하고 모델 계열별 관찰 건수를 표시한다. 구형·미측정 기록은 실패 분모에 넣지 않는다.
- 100개 해설 모두에 네 단계 긴급도, 조건부 운행 안내, 사용자가 확인할 항목, 정비사 전달 항목, 삭제 전 보존 항목을 추가했다.
- 코드 검색 화면에서 운행 중단과 빠른 점검 항목을 바로 걸러 보고, 일반 모드와 정비사 상세 모드에 맞는 내용을 나누어 표시한다.
- 100개 결과를 한꺼번에 그리지 않고 30개씩 추가 표시해 보급형 휴대폰의 화면 진입 부하를 줄였다.
- 보고서에도 대응 단계, 운행 안내, 사용자 확인, 정비사 전달, 삭제 전 보존 항목을 포함한다.
- 이전 형식의 서명 팩은 안전한 기본 안내로 계속 읽을 수 있도록 호환성을 유지했다.
- 공개 지식 팩을 100개 해설과 29개 공식 근거 문서로 확대했다. Volkswagen·Audi·Subaru 자료를 더하고 흡기·연료·점화·배출가스·변속기·통신·하이브리드·EV 공통 코드를 보강했다.
- 공통 코드 설명과 특정 제조사 공지의 적용 범위를 분리해 다른 차종에 특정 수리법이 자동 적용되지 않도록 했다.
- 차량별 저장 기록에서 아직 해설이 없는 코드를 모으는 `미해설 코드 대기함`을 추가했다. 같은 세션의 반복 응답은 중복 집계하지 않고 차량·코드·ECU별 발생 세션 수와 상태를 표시한다.
- 검증 후보 복사본은 차량 별칭을 익명화하고 VIN, 블루투스 주소, 원시 응답, 정확한 시각을 제외한다.
- 지식 팩 버전을 `2026.09.13-public.5`, 서명 리비전을 5로 올렸다.
- 검증: 단위 테스트 268건 실패/오류 0, Galaxy A23 기기 테스트 8건 통과, debug/release APK와 release Lint 성공.

## 2026-09-11 — 공개 데이터 우선 개발판

- 새 홈: 코드 검색, 차량 진단, 배터리 그래픽, 기록. 일반 사용자/정비사 도구 전환.
- 공개 문서를 대조한 오프라인 해설 3건. 북미 자료 적용 범위와 국내 미검증 상태 명시.
- 제한된 크기의 UTF-8 배터리 CSV를 로컬에서 읽고 셀 전압·온도·SOC·BMS 보고 SOH 표시.
- 결측치·비정상 숫자·중복 채널·출처/시각 누락 검사. 가상 예시는 별도 진입.
- 삭제 후 응답 실패·불완전 수신을 소거 성공으로 보고하지 않음. ECU별 코드 비교와 결과 로그 보강.
- UDS 조회 무응답을 유지하고 정상 ECU 목록에서 제외. 같은 세션 재삭제 제한.
- 차량 이름 변경 시 ECU 주소 캐시와 기존 진단 상태 무효화.
- 유료 데이터 구매·계약·공개 배포 없음. 차종별 실차 검증은 미실행.
- Galaxy A23 / Android 14: release 빌드·업데이트 설치, 검색·정비사 상세·배터리 CSV 및 그래프·잘못된 파일 거절 확인. 글씨 150% 초기 화면 확인 후 복원.

# CHANGELOG.md — Serotonin 범용 OBD (구 LeafOBD)

## 2026-09-13 — 차량별 DTC 11개 공식 문서 교차 검증

- 기존 차량별 DTC 11개 모두를 동일 적용 차량의 후속 현대 공식 문서에서 다시 확인했다.
- P00B7·P2118·P0401·P0236·P0299·P0A0D·P1B76·P1B77은 별도 차종 또는 별도 제조사 공식 문서도 연결하고, 후속 개정판과 독립 의미 대조를 화면에서 구분한다.
- 공통 코드 해설 근거 충족 5개, 제조사·적용 차량 범위 유지 5개, 표준 원문 추가 확인 1개로 승격 검토 결과를 기록했다.
- 다른 차량 문서의 고장 원인·임계값·부품 교체 절차는 현재 차량에 재사용하지 않는다.
- 검증: 단위 테스트 297건 실패/오류 0, Galaxy A23 기기 테스트 8건, debug/release APK와 release Lint 통과.

## 2026-09-13 — 대한민국 공식 사양 30개 조합 확대

- 현대·기아 국내 공식 차량 페이지와 카탈로그를 근거로 사양 카탈로그를 5개에서 30개 조합으로 확대했다.
- 내연기관·하이브리드는 엔진과 변속기를, EV3·EV6·아이오닉 5 N은 배터리 용량·모터 출력·구동방식을 함께 표시한다.
- 같은 차종에 여러 엔진이나 배터리·구동 사양이 있으면 사용자 입력 전까지 후보를 확정하지 않는다. 배기량 또는 배터리 용량과 구동방식을 입력하면 해당 사양만 남긴다.
- 공식 판매 사양과 국내 DTC 정의, 정비 절차, SOC·SOH 조회 지원은 계속 분리한다.
- 검증: 단위 테스트 292건 실패/오류 0, Galaxy A23 기기 테스트 8건, debug/release APK와 release Lint 통과.

## 2026-09-13 — 대한민국 공식 파워트레인 사양 대조

- 현대·기아 국내 공식 카탈로그와 차량 역사 페이지에서 5개 사양 항목을 별도 구조화했다.
- 2021–2022 싼타페 TM 하이브리드, 2022 니로 DE HEV/PHEV, 2024 싼타페 MX5 하이브리드, 2026 니로 하이브리드, 2026 아반떼 하이브리드의 엔진·변속기 정보를 포함한다.
- 국내 프로필의 제조사·차종·연식·동력계·세대 코드를 검사하고, 세대 코드가 없으면 정확 일치 대신 추가 확인으로 표시한다.
- 싼타페 TM과 니로 DE는 관련 북미 시스템 적용표와 연결하되 `사양 일부 겹침`과 `국내 DTC 적용 확인`을 분리했다.
- 오류코드 검색과 차량 지원 범위 화면에서 국내 공식 사양 원문과 북미 자료의 적용 한계를 함께 표시한다.
- 검증: 단위 테스트 287건 실패/오류 0, Galaxy A23 기기 테스트 8건, debug/release APK와 release Lint 통과.

## 2026-09-13 — 차량별 DTC와 파워트레인 근거 연결

- Hyundai 23-FL-001H의 P1441·P1A77·P00B7·P2118·P0401·P0236·P0299와 Hyundai 23-HC-001H의 P0A0D·P1B25·P1B76·P1B77을 해당 파워트레인 적용표에 연결했다.
- 오류코드 검색에서 선택 차량과 공개 문서 조건의 일치, 사양 부족, 판매 지역 검토 필요, 다른 차량 문서를 구분한다.
- 기존 지식 팩에 없는 코드도 제조사 공지에 등장한 사실과 원문을 보여주되, 독립적으로 검증된 일반 해설처럼 표시하지 않는다.
- Sonata YF HEV 고전압 인터록·전력 릴레이 적용표를 별도 시스템으로 추가해 Theta II 2.4 캠페인 적용표와 섞이지 않게 했다.
- 고전압 부품 분해·측정, ECU 주소, 진단 명령과 수리 절차는 추가하지 않았다.
- 검증: 단위 테스트 283건 실패/오류 0, Galaxy A23 기기 테스트 8건, debug/release APK와 release Lint 통과.

## 2026-09-13 — 현대·기아 파워트레인 적용표 1차

- 공개 제조사 문서에서 6개 파워트레인 시스템과 11개 차종·연식 적용 조건을 구조화했다.
- 현대 7단 건식 DCT, 기아 Gamma 1.6 T-GDI·7DCT, 니로 DE 하이브리드 1.6 Atkinson·6DCT, Santa Fe TM Gamma II 1.6T HEV/PHEV, Sonata YF HEV Theta II 2.4, 현대 2.5T·8단 습식 DCT를 포함한다.
- 차종, 세대 코드, 연식, 동력계, 엔진, 변속기, 판매 지역을 각각 검사한다. 입력이 부족하면 필요한 항목을 표시하고 문서 일치로 판정하지 않는다.
- 북미 공개 문서와 비슷한 대한민국 차량은 `판매 지역 적용 확인 필요`로 분리한다. 니로 EV는 니로 하이브리드 6DCT 자료에서 제외한다.
- 차량 등록 화면에서 공개 적용표 후보와 부족한 사양을 보여주고, 지원 범위 화면에서는 판정 상태·재사용 제한·원문 링크를 제공한다.
- 이 표는 자료 분류와 조사 우선순위용이다. ECU 주소, 진단 명령, 코딩·학습 기능은 추가하지 않았다.
- 검증: 단위 테스트 278건 실패/오류 0, Galaxy A23 기기 테스트 8건, debug/release APK와 release Lint 통과.

## 2026-09-13 — 차량 계열 중심 데이터베이스 기반

- 오류코드 자료 적용 범위를 국제 표준, 제조사 그룹, 플랫폼·시스템, 차종 계열, 정확한 연식·사양의 5단계로 분리했다.
- 같은 제조사나 플랫폼이라는 사실은 자료 검색 후보를 좁히는 데만 사용하며, 제조사 전용 코드·ECU 주소·수리 절차를 자동 공유하지 않는다.
- 대한민국에서 운행되는 주요 차종 인덱스를 36개에서 83개로 확대했다. 현대·기아에 제네시스, 닛산, 토요타, 쉐보레, 르노코리아, KGM 계열을 추가했다.
- 공식 공개 자료로 확인한 현대차그룹 E-GMP 일부 모델과 닛산 리프 ZE0/AZE0/ZE1 세대를 별도 시스템 관계로 표시하고, 사용자 대상 니로 DE 계열은 2019 EV 정확 적용 범위와 분리한다.
- `아이오닉`과 `아이오닉 5`처럼 이름이 겹칠 때 더 구체적인 별칭을 우선해 잘못된 계열 분류를 막았다.
- 지원 범위 화면에서 현재 차량에 적용된 각 데이터 계층, 재사용 조건과 공개 분류 근거를 확인할 수 있다.
- 진단 명령, ECU 주소, 오류코드 삭제 절차와 지식 팩 내용은 변경하지 않았다.
- 검증: 단위 테스트 273건, debug/release APK, release Lint와 Galaxy A23 기기 테스트 8건 통과. 기기에는 정식 release APK를 업데이트 설치했다.

## 2026-08-04 — 앱 이름 변경, 배포 준비 (v1.1, versionCode 2)

### 이름 변경 — LeafOBD → Serotonin 범용 OBD

앱이 Nissan Leaf 전용을 넘어 표준 OBD-II 차량과 UDS 응답 차량을 두루 다루게 되어
표시 이름을 바꿨다. **패키지 ID(`com.eunho.leafobd`)는 유지**해 기존 설치·서명과 호환된다.

- 표시 이름: `Serotonin 범용 OBD` (런처 아이콘은 짧은 `Serotonin OBD`)
- 로그 파일 접두사: `LeafOBD_` → `SerotoninOBD_`
- 리포트·JSON·클립보드의 앱 이름 문자열 교체
- 내부 코드 식별자(테마·클래스명)는 그대로 두었다

### 추가 — 제작자 정보

앱 정보 화면에 제작자(이은호), 문의(serotonin.1207@gmail.com),
소스 코드(GitHub) 링크를 추가했다. `LICENSE`(MIT + 안전 고지) 추가.

### 추가 — 업데이트 확인 (규칙 개정)

이 앱은 원래 인터넷 권한 자체를 금지했다. 제작자 승인 하에 규칙을 개정하고
**업데이트 확인 목적으로만** 인터넷 권한을 추가했다.

- `UpdateChecker` — GitHub `version.json` 에서 최신 **버전 번호만** 읽는다
- 새 버전이 있으면 홈 화면에 안내, "다운로드" 를 누르면 브라우저로 넘어간다
  (앱이 직접 설치하지 않는다)
- **진단 데이터(오류코드·로그·VIN)를 전송하는 코드는 없다**
- 설정에서 끄면 인터넷을 전혀 쓰지 않는다
- `AndroidManifest`, `CLAUDE.md` 규칙 7, `SAFETY.md` 를 이 변경에 맞게 정직하게 개정

### 추가 — 배포 문서

`DISTRIBUTION.md` — 서명 키 생성, GitHub Releases, version.json 갱신,
구글 드라이브 배포까지 전체 절차.

### 테스트

171건 유지, 전부 통과. (앱 이름 변경에 맞춰 로그 형식 테스트 갱신)

## 2026-08-04 — 삭제 화면 혼동 제거, 스캔 신뢰성 개선

### 문제 — 사용자가 잘못된 삭제 화면을 썼다

실차 로그에서 확인:

```
명령: 04  →  NO DATA
삭제 시도 여부: 예 / 삭제 응답: NO DATA
```

**표준 OBD Mode 04 삭제 화면**을 쓴 것이었다. 이 차량은 표준 OBD 에 응답하지 않으므로
당연히 아무 일도 일어나지 않는다. ECU 주소로 읽은 코드는 **UDS 14** 로 지워야 한다.

화면 이름이 비슷해 구분되지 않은 것은 설계 실수다. 고쳤다.

- 표준 OBD 삭제 화면 최상단에 **"이 화면은 이 차량에 맞지 않습니다"** 경고와
  ECU 오류코드 삭제 화면으로 가는 버튼 추가
- 홈 화면에 **"ECU 오류코드 삭제"** 버튼을 직접 노출

### 문제 — 스캔이 엉뚱한 주소를 응답으로 기록했다

`ATSH71D` 요청에 `72D` 가 응답하는 등, **요청 주소와 응답 ID 가 어긋난 사례**를 확인했다.
응답 대기(`ATST20` = 128ms)가 짧아 느린 ECU 의 답이 다음 주소를 물어본 뒤 도착하고,
그것이 다음 주소의 응답으로 기록되고 있었다.

- `ATST20` → **`ATST32`** (200ms)
- 스캔 요청 대기 1200ms → **2000ms**
- 주소 간 **정착 시간 120ms** 추가 — 늦은 응답을 다음 요청 전에 흘려보낸다

### 변경 — 찾은 ECU 주소 기억

전체 구간 스캔은 수 분이 걸리는데 그 사이 차량 모듈이 절전에 들어가
정작 조회할 때는 응답이 없어지는 일이 있었다(14:07 성공 → 14:09 실패).

- 새로 훑어 찾은 주소만 `knownEcuAddresses` 로 저장
- 다음 진단은 그 주소만 확인 → **수 분에서 20초로 단축**
- 저장 목록의 **절반 미만만 응답하면** 목록을 믿지 않고 다시 훑는다
- 기존 목록을 확인만 한 경우에는 목록을 덮어쓰지 않는다(절전으로 줄어든 것일 수 있다)
- 이번 스캔이 비어도 저장된 주소로 조회·삭제를 시도할 수 있다

## 2026-08-04 — 진단 흐름 통합, ECU 삭제와 DID 스캔

### 변경 — 진단 흐름 통합 (12단계)

"진단 실행" 한 번으로 끝까지 진행한다.
표준 OBD 로 코드가 나오지 않으면 **자동으로 ECU 스캔 → UDS 오류코드 읽기**로 넘어간다.

```
1~4  연결 · 초기화 · 어댑터 정보 · 전압
5~6  차량 정보 · 프리즈 프레임
7~9  표준 OBD (Mode 03/07/0A)
10   ECU 응답 스캔      ← 표준에서 코드가 없을 때만
11   ECU 오류코드 (UDS) ← 응답한 ECU 대상
12   로그 저장
```

전기차는 10~11 로, 일반 차량은 7~9 에서 끝난다. 설정에서 끌 수 있다(`autoEcuScan`).

### 추가 — ECU 오류코드 삭제 (`UdsClear`)

ISO 14229 service `0x14` ClearDiagnosticInformation (`14 FF FF FF`).

**이 명령의 성격을 문서와 화면에 명확히 적었다.**
저장된 고장 기록을 지우고 ECU 가 다시 판단하게 하는 것이며,
**고장 감지 기능을 끄지 않는다.** 원인이 남아 있으면 곧바로 다시 검출된다.
접촉기 강제 작동이나 충전 강제 시작 같은 명령은 여전히 구현하지 않는다.

표준 OBD 삭제와 동일한 안전 절차를 적용했다.

- 사전조건 6가지 (`UdsClearEligibility`) — 읽기·저장 선행, 세션당 1회
- 안전 확인 4개 + 확인 문구 입력
- 고전압 계통 코드가 포함되면 **추가 경고 카드**
- 삭제 후 3초 대기 → 자동 재조회 → 전후 비교
- 재발 시 "ECU 가 지금도 고장을 감지하고 있습니다" 강조

### 추가 — DID 스캔 (`DidScanner`)

`22`(ReadDataByIdentifier)로 읽히는 데이터 항목을 찾는다.
부품번호·소프트웨어 버전 등을 확보해 수리 업체에 전달하기 위한 것이다.

- 표준 식별 구간 `F180~F1FF`, 넓게 스캔 시 `0000~01FF` 추가
- ISO 14229 표준 이름 17종 한국어 표기
- ASCII 로 읽히면 문자열로 함께 표시
- 읽기 전용 — 어떤 값도 바꾸지 않는다

### 테스트

159건 → **169건**. `UdsClearTest` 10건 신규.

## 2026-08-04 — 오류코드 확보, 프레임 유실 감지

### 실차 결과 — 오류코드를 읽었다

흐름 제어 적용 후 다중 프레임이 완성되어 실제 코드를 확보했다.

| ECU | 코드 | 상태 |
|---|---|---|
| `79A` | **P3180-97, P317E-97** | 0x0B — 현재 고장 + 확정 |
| `7BB` | **P33ED-00** | 0x0C — 보류 + 확정 |
| `764` | B2630-11, B2631-15, B27A0~B27A4-79 | 0x10 — 삭제 후 검사 미완료 |

### 수정 — 프레임 유실로 엉뚱한 코드가 만들어지던 문제

`764` 응답(51바이트)에서 **연속 프레임 순번 25가 유실**되었다.
그런데도 앱은 남은 바이트를 그대로 이어 붙여 해석했고,
정렬이 어긋나 `P10A7-A8`, `P10FF-FF` 같은 **존재하지 않는 코드**를 만들어 냈다.

진단 도구에서 잘못된 코드는 코드가 없는 것보다 나쁘다. 세 가지를 고쳤다.

**1. 순번 검사** — `ObdFrameParser` 가 연속 프레임 순번을 추적한다.
건너뛰면 그 지점을 `gapAfter` 로 기록하고, 파서는 **유실 지점 앞까지만** 해석한다.

**2. 프레임 간격** — 흐름 제어 STmin 을 0 에서 **20ms** 로 바꿨다.
0 이면 ECU 가 몰아서 보내는데 Bluetooth 구간이 못 따라가 프레임이 빠진다.

**3. 자동 재시도** — 유실이나 길이 부족을 감지하면 간격을 **50ms** 로 넓혀 한 번 더 읽는다.
더 온전한 쪽을 채택한다.

`EcuResponse` 에 `declaredLength` / `gapAfter` / `complete` / `usableBytes` 를 추가했다.

### 추가 — 로그에 ECU 오류코드 기록

`DiagnosticSession.udsResults` 추가. TXT 로그에 ECU별 코드, 상태 비트,
원본 바이트가 남는다. 정비소에 그대로 전달할 수 있다.

### 테스트

156건 → **159건**. 실차에서 유실이 발생한 `764` 응답을 그대로 회귀 테스트로 넣어,
가짜 코드(`P10A7`, `P10FF`)가 다시 생기지 않는지 검사한다.

## 2026-08-04 — 실차 통신 성공, UDS 오류코드 읽기

### 실차 결과 — 23개 ECU 응답

ECU 응답 스캔을 실제 2019 Leaf 에서 실행한 결과 **23개 주소가 응답했다.**

```
700 70F 72D 72E 735 73E 760 762 763 764 765
76D 772 778 77D 78C 793 79A 7B6 7BA 7BB 7BD 7D5
```

지금까지 모든 요청이 `NO DATA` 였던 이유가 확정되었다.
**방송 주소(7DF)로만 물어봤기 때문이었다.** 주소를 지정하니 응답한다.
게이트웨이는 막혀 있지 않았다.

수집한 응답 중 오류코드가 실제로 있는 곳:

| ECU | 응답 | 내용 |
|---|---|---|
| `7BB` | `59 02 0E 33 ED 00 0C` | **P33ED-00** 1건 (단일 프레임, 완전) |
| `79A` | `10 0B 59 02 0F 31 80 97` | 2건 (다중 프레임, 잘림) |
| `764` | `10 33 59 02 19 A6 30 11` | **약 12건** (다중 프레임, 잘림) |

### 수정 — 다중 프레임이 잘리던 문제

`ATSH` 로 주소를 직접 지정하면 어댑터가 흐름 제어 프레임의 주소를 알 수 없어
다중 프레임 응답의 첫 프레임만 받고 끝났다.

`ATFCSH<주소>` / `ATFCSD300000` / `ATFCSM1` 으로 흐름 제어를 직접 지정했다.
모두 ELM327 표준 명령이다.

### 추가 — `UdsDtcParser`

`19 02`(ReadDTCInformation) 응답을 오류코드 목록으로 해석한다.

- UDS 는 **3바이트 DTC + 1바이트 상태** 구조 (표준 OBD 의 2바이트와 다르다)
- 앞 2바이트를 SAE J2012 문자 코드로, 세 번째를 고장 유형(FTB)으로 표시 → `P33ED-00`
- 상태 비트 8종을 한국어로 변환 (확정됨 / 보류 중 / 현재 고장 상태 / 경고등 점등 요청 …)
- 응답이 덜 왔으면 `truncated` 로 표시해 흐름 제어 문제를 알린다

### 추가 — `UdsDiagnostics` 와 ECU 오류코드 화면

스캔에서 찾은 주소들을 순회하며 `19 02` 로 코드를 읽는다.

- **확정 또는 현재 고장 상태인 코드를 따로 모아 상단에 강조**
- ECU별 결과, 코드 없는 ECU 목록
- 정비소 전달용 텍스트 복사
- 보내는 요청은 `19 02` 와 `22 F1 90` 뿐. 삭제·쓰기·세션 제어·보안 접근 없음

### 테스트

143건 → **156건**. `UdsDtcParserTest` 13건 신규.
실차에서 받은 `7BB` 응답을 그대로 테스트 자료로 썼다.

## 2026-08-04 — ECU 응답 스캔 (읽기 전용 UDS)

### 배경 — 아직 안 해본 것이 하나 있었다

지금까지 모든 요청을 **방송 주소(7DF)** 로만 보냈다.
게이트웨이가 있는 차량은 방송 요청을 통과시키지 않을 수 있는데,
**개별 ECU 주소를 직접 지정하는 방식(`ATSH`)은 한 번도 시도하지 않았다.**

실차 로그에서 프로토콜 6은 버스 오류가 없었다(물리 계층은 정상).
따라서 "주소를 지정하면 응답할지"는 아직 열려 있는 질문이었다.

### 추가 — `EcuScanner`

`ATSH` 로 ECU 주소를 하나씩 지정하며 표준 UDS 읽기 요청을 보낸다.

- **빠른 스캔** — 표준 OBD 주소 `7E0`~`7E7` 8개
- **전체 스캔** — 진단 주소 구간 `0x700`~`0x7EF` 240개
- 2단계 방식: 가벼운 존재 확인(`3E00`) 후, 응답한 주소에만 실제 읽기 요청
- `ATST` 로 응답 대기 시간을 줄여 전체 스캔 시간을 단축
- 끝나면 요청 주소를 방송(`7DF`)으로, 대기 시간을 기본값으로 되돌린다

### 보내는 요청 — 전부 ISO 14229 표준 읽기

| 요청 | 서비스 | 내용 |
|---|---|---|
| `3E 00` | TesterPresent | 존재 확인 |
| `22 F1 90` | ReadDataByIdentifier | 차대번호 |
| `19 02 FF` | ReadDTCInformation | 오류코드 |

**의도적으로 넣지 않은 것** (테스트로 강제한다):
`0x10` 세션 제어, `0x27` 보안 접근, `0x2E` 쓰기, `0x31` 루틴 실행, `0x11` ECU 리셋.

### 핵심 판정 — 부정 응답도 성과다

`7F <서비스> <NRC>` 부정 응답은 **ECU 가 거기 존재한다는 증거**다.
무응답과 전혀 다르므로 별도 상태(`NEGATIVE`)로 구분하고,
NRC 를 ISO 14229 표준 설명으로 번역해 보여 준다.

### 테스트

130건 → **143건**. `EcuScanTest` 13건 신규.
"금지된 서비스를 보내지 않는다"를 테스트로 못 박았다.

## 2026-08-03 — CAN 버스 확인(읽기 전용) 및 조사 결과 반영

### 추가 — CAN 버스 확인 화면

`ATMA`(Monitor All)로 정해진 시간 동안 버스를 **듣기만** 한다.
차량에 어떤 요청도 보내지 않으며, 수신한 CAN ID와 프레임 수만 집계한다.

- `Elm327Client.monitor()` — 프롬프트를 돌려주지 않는 수신 모드 전용 경로
  (종료 시 보내는 캐리지리턴 한 글자는 어댑터를 멈추는 신호이며 버스로 나가지 않는다)
- `CanMonitorParser` — CAN ID별 집계, 상태 문자열 제외, 원시 데이터 길이 제한
- 홈 화면과, 표준 조회가 전부 비었을 때 진단 화면에서 진입
- 프레임 0건일 때 **"차량이 꺼져 있다"고 단정하지 않는다** —
  2018년 이후 Leaf는 게이트웨이가 브로드캐스트를 막는 것으로 알려져 있다

### 문서 — 왜 표준 OBD로는 Leaf 데이터를 못 읽는가

`nissan/README.md` 에 공개 자료 조사 결과를 출처와 함께 정리했다.

- Leaf 는 배출가스 규제 대상이 아니라 **표준 OBD PID 준수 의무가 없다** →
  `NO DATA` 가 정상 동작
- 2018년 이후(ZE1) 는 **OBD-II 포트가 CAN 게이트웨이로 분리** →
  브로드캐스트 트래픽이 포트에 오지 않음
- 진단 데이터는 **UDS 유사 service 0x21** 요청으로만 가능 (LeafSpy 방식)
- 확인된 프로토콜은 **ATSP6**

추측한 ECU 주소는 코드에 넣지 않았다. 검증 조건은 그대로 유지한다.

### 테스트

123건 → **130건**. `CanMonitorParserTest` 7건 신규.

## 2026-08-03 — 프로토콜 수동 지정 및 자동 순차 시도

### 배경 — 실제 차량에서 통신 실패

2019 Leaf 에 연결한 3회 시도가 모두 동일하게 실패했습니다.

```text
ATZ ~ ATRV  전부 성공 (ELM327 v2.3, 13.3V)
ATDP        AUTO      ← 프로토콜을 끝내 정하지 못함
0101, 0100, 0902, 020000, 03, 07, 0A
            → SEARCHING... UNABLE TO CONNECT
```

어댑터·전원은 정상인데 **자동 프로토콜 탐색(`ATSP0`)이 실패**한 상태였습니다.
ELM327 클론에서 흔한 증상이며, 프로토콜을 직접 지정하면 통신되는 사례가 많습니다.

### 추가 — `ObdProtocol`

ELM327 데이터시트에 공개된 표준 프로토콜 11종(0~A)을 열거형으로 정의.
`ATSP<n>` / `ATDP` 는 모두 공개 표준 명령이며 제조사 전용 명령이 아닙니다.

### 추가 — `ProtocolProbe`

초기화 직후 표준 요청 `0100` 으로 **실제 통신 가능 여부**를 확인합니다.

- 실패하면 CAN 계열부터 순서대로 시도: 6 → 7 → 8 → 9 → 5
- 5보 초기화 계열(3, 4)은 한 번에 5초 이상 걸려 기본 후보에서 제외 (수동 선택은 가능)
- `NO DATA` 는 **연결됨(데이터 없음)** 으로 구분 — `UNABLE TO CONNECT` 와 원인이 다름
- 순차 시도로 찾아내면 "설정에서 고정하면 더 빠릅니다" 안내

### 추가 — 설정

- **연결 프로토콜** 선택 (자동 + 11종)
- **실패 시 순차 시도** 토글 (기본 켬)
- 지난 진단의 프로토콜 탐색 결과 표시

### 변경

- 초기화 단계가 프로토콜 탐색 결과에 따라 성공/주의/실패로 갈린다
  - 통신 실패 시 시도한 프로토콜 목록과 함께 원인 안내
- `DiagnosticSession.protocolAttempts` — 시도 내역을 로그에 기록
- 모의 어댑터에 `ATSP<n>` 처리와 **자동 프로토콜 실패** 시나리오 추가
  (실차 증상 재현: 자동은 실패, 6번으로 고정해야 통신)

### 테스트

113건 → **123건**, 전부 통과. `ProtocolProbeTest` 9건 신규.

### 정리

기기에 남아 있던 0바이트 로그 파일 10개 삭제 (이전 복원 실패로 생긴 빈 파일).

## 2026-07-30 — 배포 준비

지인에게 나눠 줄 수 있도록 배포용 빌드와 호환성을 정비했습니다.

### 추가 — release 서명 설정

- `keystore.properties`(버전 관리 제외)에서 서명 정보를 읽는 Gradle 설정
- `keystore.properties.example` 와 `.gitignore` 규칙(`*.jks`, `keystore.properties`)
- release 빌드 검증 완료: **8.2MB, 서명됨, `debuggable` 속성 없음**
  - 실기기에서 `adb run-as` 가 `package not debuggable` 로 차단되는 것까지 확인
  - debug APK(12MB, `debuggable=true`)는 앱 내부 진단 로그를 꺼낼 수 있으므로 배포 금지
- 코드 축소·난독화는 켜지 않았다. 오류 메시지의 클래스 이름이 남아야
  사용자가 상황을 설명할 수 있고, 진단 앱에서는 그게 용량보다 중요하다.

### 추가 — insecure RFCOMM 폴백

일부 ELM327 클론 어댑터는 페어링 암호화를 요구하는 보안 소켓으로 연결되지 않는다.
보안 소켓 실패 시 `createInsecureRfcommSocketToServiceRecord` 로 한 번 더 시도한다.
Android 공식 공개 API 이며, 대상은 사용자가 직접 페어링한 장치뿐이다.
(지시서가 금지한 리플렉션 우회와는 다르다)

두 방식 모두 실패하면 **1차(보안 소켓) 실패 사유**를 보여 준다. 원인에 더 가깝기 때문이다.

### 추가 — 앱 아이콘

기본 템플릿 아이콘을 잎 + OBD 커넥터 도안으로 교체했다.
적응형 아이콘 안전 영역(72x72) 안에 그렸고, monochrome 으로도 형태가 남게 했다.

### 추가 — INSTALL.md

지인에게 그대로 전달할 수 있는 설치 안내문.
어댑터 고르는 법(BLE 제품 주의), 출처 불명 앱 허용, Play Protect 경고,
삭제 기능 주의사항, 문제 해결표를 담았다.

### 확인

- 다크 모드 정상
- 런처 아이콘 정상 표시
- release 빌드 실행 및 크래시 없음

## 2026-07-30 — 실기기 검증 및 수정

갤럭시 A23(SM-A235N, Android 14)에서 adb 로 화면을 조작하며 전 기능을 확인했습니다.

### 수정 — 프리즈 프레임 단계가 실패를 성공으로 표시하던 문제

무응답 시나리오로 실기기 확인 중 발견했습니다.
`020000` 이 타임아웃돼 빈 응답만 남았는데도 6단계가 **"성공"** 으로 표시되었습니다.
`FreezeFrame.isEmpty` 가 원시 응답 유무까지 포함해 판단했기 때문입니다.

- `FreezeFrame.hasData` 추가 — 실제로 해석된 값(원인 DTC 또는 PID 값)이 있는지만 본다
- 진단 단계 상태를 통신 성공 여부로 판정하도록 변경 (실패 시 **실패** + 타임아웃 메시지)
- 삭제 화면의 "프리즈 프레임 확보" 표시도 `hasData` 기준으로 변경
- TXT 로그에 "해석된 값이 없습니다" 경고 추가
- 회귀 테스트 추가 (`무응답 시나리오의 프리즈 프레임은 사용할 수 있는 값이 없다`)

삭제 전 확보 여부를 판단하는 근거이므로, 실패를 성공으로 보여주면 안 되는 자리였습니다.

### 실기기 확인 결과 (모두 정상)

- 10단계 진단 전 과정, 프리즈 프레임 원인 DTC `P0AA6` 표시
- 경고등 점등 · ECU 보고 코드 4건 · 지원 PID 7개 · 실시간 값 4종
- VIN 화면 표시 `SJNFAAZE0U6012345` / 파일 저장 `SJN**********2345`
  (TXT·JSON 어디에도 전체 VIN 없음을 grep 으로 확인)
- 삭제 조건 6가지 중 4충족 2미충족 → 버튼 잠김
- 진단 기록 3건, TXT/JSON 공유 버튼
- 설정이 앱 재시작 후에도 유지됨
- 무응답 시나리오에서 타임아웃 후에도 앱이 멈추거나 죽지 않음 (FATAL 0건)

### 미확인

- 확인 문구 입력 후 삭제 실행 — adb 로 한글 입력이 불가해 직접 확인 필요
- CAN 헤더 켠 상태의 재조립 — 모의 응답에 헤더가 없어 실차에서만 검증 가능

## 2026-07-30 — 표준 명령 확장 (v1.0, versionCode 1)

표준 OBD-II(SAE J1979) 범위 안에서 조회 항목을 넓혔습니다.
**추측한 제조사 전용 명령은 하나도 추가하지 않았습니다.**

### 추가 — Mode 02 프리즈 프레임 (가장 중요)

- `020000` 지원 PID, `020200` 원인 DTC, 지원 PID별 값 조회
- **DTC 읽기보다 먼저** 실행한다. Mode 04 로 삭제하면 차량에서 함께 사라지므로,
  로그 파일이 유일한 사본이 된다.
- 해석하지 못한 PID도 원시 응답을 그대로 보존한다
  (나중에 검증된 자료가 생기면 저장된 원시값으로 다시 해석할 수 있어야 하므로)
- 삭제 화면에 "프리즈 프레임 확보" 상태를 표시
- 이전 버전은 "삭제하면 진단 정보가 사라진다"고 경고만 하고 정작 그 정보를 저장하지
  않았다. 그 구멍을 메운 변경이다.

### 추가 — Mode 01

- `0101` — 경고등(MIL) 점등 여부와 ECU가 보고하는 DTC 개수
  (Mode 03 결과와 교차 확인할 수 있다)
- `0100` / `0120` / `0140` … — 지원 PID 비트맵을 이어서 조회.
  지원하지 않는 PID를 묻지 않게 되어 불필요한 타임아웃이 줄어든다.
- 해석 가능한 표준 PID 15종 (`Pid` 열거형, SAE J1979 공개 계산식만 사용)
  - 제어 모듈 전압(`0142`)은 ATRV 와 달리 ECU 가 직접 보고하는 값이다

### 추가 — Mode 09 VIN

- `0902` 다중 프레임 ASCII 파싱
- **기본값 꺼짐** — VIN 은 차량을 특정할 수 있는 식별정보다
- 화면에는 전체 표시, 파일에는 마스킹 저장(`SJN**********2345`)
- 마스킹 여부는 설정에서 변경 가능

### 추가 — CAN 헤더 표시 (ATH1)

- `ObdFrameParser` — ISO-TP(ISO 15765-2) 프레임 재조립
  - 단일 프레임(`0N`), 최초 프레임(`1N`), 연속 프레임(`2N`) PCI 해석
  - 11비트(3자리) / 29비트(8자리) CAN ID 자동 판별
  - ECU별로 응답을 나누어 각 DTC에 보고 ECU를 기록
- 헤더를 켜면 ELM327 이 프레임을 합쳐 주지 않으므로 앱이 직접 재조립한다
- 설정에서 토글, 기본값 꺼짐

### 추가 — 설정 영속화

- `data/AppSettings` + `SettingsRepository` (SharedPreferences, 외부 라이브러리 없음)
- **차량 이름 변경 가능** — 다른 OBD-II 차량에도 이 앱을 쓸 수 있다
- 조회 항목 토글: 프리즈 프레임 / 실시간 값 / CAN 헤더 / VIN / VIN 마스킹
- 테스트 모드와 시나리오도 재실행 시 유지된다

### 변경

- 진단 단계가 8단계 → **10단계**
  (차량 정보 확인, 프리즈 프레임 저장이 DTC 읽기 앞에 추가)
- `DiagnosticSession` 에 vehicle / vin / monitorStatus / supportedPidsHex /
  liveValues / freezeFrame / headersOn 추가 → TXT·JSON·복사용 요약에 모두 반영
- `DtcCode` 에 `ecu` 추가
- 진단·삭제 화면에서 **진단 기록으로 바로 이동**하는 버튼 추가
  (이전에는 홈에서만 갈 수 있었다)
- 모의 어댑터가 Mode 01/02/09 에도 응답. 삭제하면 프리즈 프레임도 사라지도록 재현

### 테스트

75건 → **113건**, 전부 통과.

| 파일 | 건수 |
|---|---|
| `obd/DtcParserTest` | 24 |
| `obd/VehicleInfoParserTest` | 21 (신규) |
| `elm327/Elm327ClientTest` | 16 |
| `log/LogFormatTest` | 13 |
| `viewmodel/ClearEligibilityTest` | 11 |
| `obd/ObdFrameParserTest` | 10 (신규) |
| `util/ResponseTextTest` | 10 |
| `obd/VoltageParserTest` | 7 |
| 템플릿 `ExampleUnitTest` | 1 |

### 넣지 않은 것

- **Mode 08** (온보드 시스템 제어) — 읽기가 아니라 장치를 작동시키는 명령이라 제외
- **`ATSH` / `ATCRA` + 임의 ECU 주소** — 검증된 주소 없이 사용하지 않음
- Mode 05(산소센서), Mode 06 — 전기차에서 의미가 없음
- Nissan 전용 명령 — `nissan/README.md` 조건 미충족



## 2026-07-29 — 1차 버전 (v1.0, versionCode 1)

Android Studio 기본 템플릿에서 시작해 1차 목표 전체를 구현했습니다.

### 프로젝트 설정

- Android Studio 생성 프로젝트를 `C:\python\leaf EV 프로젝트` 로 이전
- compileSdk 36.1 → **37** (최신 AndroidX `core 1.19`, `lifecycle 2.11` 요구사항)
- 의존성 추가: navigation-compose 2.9.8, lifecycle 2.11.0(runtime/viewmodel-compose),
  kotlinx-coroutines 1.11.0, coroutines-test
- 의존성 갱신: core-ktx 1.19.0, activity-compose 1.13.0, androidx-test 1.3.0/espresso 3.7.0
- `buildConfig = true` 활성화 (로그에 앱 버전 기록)

### 한글 경로 대응

- `android.overridePathCheck=true` 추가 — AGP의 비ASCII 경로 차단 해제
- **빌드 출력 폴더를 `C:\python\leafobd-build` 로 자동 이전** —
  한글 경로에서 Gradle 테스트 워커가 클래스패스를 해석하지 못해
  모든 테스트가 `ClassNotFoundException` 으로 실패하던 문제를 해결
  (경로가 ASCII 면 이 동작은 자동으로 꺼짐)

### 추가 — 통신 계층

- `bluetooth/SppConstants` — 표준 SPP UUID, 15초 타임아웃, 어댑터 이름 힌트
- `bluetooth/BluetoothPermissions` — Android 12+ `BLUETOOTH_CONNECT` 분기,
  검색(discovery)과 위치 권한은 사용하지 않음
- `bluetooth/BluetoothClassicManager` — 페어링 목록 조회, RFCOMM 연결,
  연결 전 `cancelDiscovery()`, 실패 유형별 한국어 안내
- `bluetooth/BluetoothElm327Transport` — `available()` 기반 **논블로킹** 읽기
  (블로킹 읽기는 코루틴 타임아웃으로 취소할 수 없어 사용하지 않음)
- `elm327/Elm327Client` — `Mutex` 직렬 명령 큐, `>` 프롬프트 기반 응답 리더,
  전송 전 잔여 버퍼 비우기, 모든 예외를 실패 `CommandLog` 로 변환
- `elm327/Elm327Command` — ATZ/ATE0/ATL0/ATS0/ATH0/ATSP0/ATI/ATRV(+선택 ATDP)
- `elm327/Elm327Initializer` — 초기화 시퀀스, 실패 명령 이름 보고, ATZ 후 안정화 대기
- `elm327/FakeElm327Transport` — 5가지 모의 시나리오

### 추가 — 진단 계층

- `obd/DtcParser` — echo·SEARCHING·프롬프트·공백·CRLF·다중 프레임 정규화,
  CAN 개수 바이트 형식과 KWP 무개수 형식 자동 판별, 0000 패딩 제거,
  잘린 응답 표시, `NO DATA` 를 오류가 아닌 "데이터 없음"으로 구분
- `obd/ObdService` — Mode 03/07/0A 읽기, Mode 04 **1회** 삭제(재시도 없음)
- `obd/VoltageParser` — ATRV 해석 및 저전압 참고 안내
- `util/ResponseText` — 응답 정규화와 오류 키워드 한국어 변환

### 추가 — 로그

- `log/DiagnosticSession`, `log/CommandLog` 모델
- `log/SessionFormatter` — TXT/JSON/복사용 요약(지시서 22장 양식)
- `log/DiagnosticLogRepository` — 앱 내부 `files/logs/` 저장·조회·삭제
- `log/LogExporter` — FileProvider 공유, 클립보드 복사
- `util/MacMasking` — MAC 주소 마지막 2바이트만 보존
- `util/Json` — 외부 라이브러리 없는 소형 JSON 작성기

### 추가 — 화면

- Material 3 테마를 Leaf 녹색으로 교체, **다이내믹 컬러 비활성**
  (배경화면에 따라 경고 색이 흐려지지 않도록)
- 홈 / 어댑터 선택 / 진단 / 오류코드 삭제 / 진단 기록 / 설정 / 사용 절차 화면
- 안전 경고 카드를 홈·진단·삭제·사용절차에 표시
- 삭제 화면: 빨간 경고 카드 + 체크박스 3개 + 확인 문구 입력 +
  미충족 조건 표시 + 삭제 전후 비교 + 재발 코드 강조
- 모든 문자열·오류 메시지 한국어
- 테스트 모드에서 "모의 데이터" 배지 상시 표시

### 추가 — 안전 장치

- `viewmodel/ClearEligibility` — 삭제 사전조건 6가지, 반복 삭제 차단
- 조건 미충족 시 버튼 비활성 + ViewModel에서 2차 검증
- 삭제 후 3초 대기 → 자동 재조회 → 전후 비교 → 결과 저장
- `nissan/` 모듈은 인터페이스와 문서만 두고 `ENABLED = false` 로 잠금

### 추가 — 테스트 (총 75건, 전부 통과)

| 파일 | 건수 |
|---|---|
| `obd/DtcParserTest` | 24 |
| `log/LogFormatTest` | 13 |
| `viewmodel/ClearEligibilityTest` | 11 |
| `util/ResponseTextTest` | 10 |
| `elm327/Elm327ClientTest` | 9 |
| `obd/VoltageParserTest` | 7 |
| 템플릿 `ExampleUnitTest` | 1 |

### 추가 — 문서

- `README.md`, `TODO.md`, `CHANGELOG.md`, `ARCHITECTURE.md`, `SAFETY.md`, `TESTING.md`
- `CLAUDE.md`, `screenshots/README.md`
- `app/src/main/java/com/eunho/leafobd/nissan/README.md`

### 수정

- `AndroidManifest.xml` — Bluetooth 권한 버전별 분기, `neverForLocation` 플래그,
  FileProvider 등록, 인터넷 권한 미선언
- `MainActivity` — 권한 런처, 시스템 설정 이동, Compose 트리만 담당

### 실기기 검증 (2026-07-29)

- 삼성 갤럭시 A23 **SM-A235N**, Android 14 / API 34 에 설치 및 실행 확인
- 테스트 모드 "삭제 후 재발" 시나리오 전체 흐름 정상, 크래시 없음
- 문서의 모델명을 SM-A234N → SM-A235N 으로 정정

### 알려진 제한

- 실제 어댑터·차량 검증 미완료 (어댑터 도착 후 진행)
- 권한 거부 / Bluetooth 꺼짐 / 화면 회전 시나리오는 실기기 확인 전
- 진단 기록 화면으로 가는 경로가 홈 화면에만 있음
- Mode 03 응답의 개수 바이트 유무는 휴리스틱으로 판별하므로
  드물게 오판할 수 있음 → 원시 응답을 항상 함께 확인할 것
- Nissan Leaf EV 전용 오류는 표준 OBD-II로 읽히지 않을 수 있음

2026-09-11 최종 검증: 단위 테스트 181 건 통과, assembleDebug 성공. 연결 기기/AVD 없음으로 UI·실차 검증 미실행.

## 2026-09-11 니로·리프 배터리 읽기 시험

- 2019 리프 ZE1 40kWh급 / 니로 EV DE 64kWh급 프로파일과 읽기 요청 추가. OVMS MIT 근거 및 오프셋: EV_PROTOCOLS.md.
- 기본 꺼짐, 사용자 선택·정차 확인 후 한 번 읽기. 조회 종료 후 연결 해제. 실차 검증 전 시험 기능.
- SOC/SOH, 96/98개 전압 채널, 별도 온도 센서를 그래프에 연결. 누락/잘린/순서가 틀린 프레임은 배제.
- 온도 센서를 셀 번호에 잘못 연결하지 않으며, 니로 음수 온도 인코딩은 미확인. 리프 온도는 환산값.
- 별도 원시 기록에 DTC 미조회 표시. 화면 회전 때 배터리 값 유지, 프로세스 종료 후 그래프 복원은 미구현.
- 남은 작업: 두 실차와 Vgate iCar Pro BT3.0 통신 및 기준 진단기 대조, 실패 로그 검토, 재연결 회귀.
## 2026-09-11 실차 검증 전 사용성 보강

- 조회 항목/진행률/실패 후 행동을 표시하고 현재 명령 종료 후 중단하는 버튼 추가. 중단 전 응답과 설정 복원 로그 보존.
- 배터리 직접 조회 결과 자동 저장. CSV는 수동 저장. 앱 종료 후 저장 기록에서 그래프/원시 응답 다시 열기.
- Android noBackupFilesDir에 원자적 교체로 저장하여 클라우드 자동 백업에서 제외. 새 배터리 기록의 외부 공유 경로 없음.
- 리프/니로 기록과 선택 설정 분리. 외부 CSV는 별도 구분이며 이름만 보고 자동으로 차량에 연결하지 않음. 가상 예시 저장 금지.
- 최저/최고 채널, 온도 차이, 전압 차이 확대 눈금 추가. 미수신 채널은 정상값으로 보정하지 않음.
- 이전 측정값/미래 시각 안내 및 같은 프로파일의 이전 직접 측정과 SOC/SOH 수치 비교. 차이는 열화·수리 효과 판정이 아님.
- 손상된 저장 파일은 다른 기록과 분리하여 오류 건수 표시. 저장 파일의 형식 버전/크기/채널/숫자/차량 일치 검사.
- 기존 원시 진단 로그의 공유·백업 설정은 이번 새 배터리 기록 저장소와 별개이며, 공유 개인정보 보호 전반은 후속 검토 대상.
검증: 단위 테스트 205건 실패/오류 0, debug/release 및 lintVitalRelease 통과. Galaxy A23 업데이트, CSV 저장 후 프로세스 종료/재열기, 차량 분류, 누락 채널/확대 그래프, 150% 글씨, 시험 기록 삭제 확인. 실제 차량 명령은 실행하지 않았다.

## 2026-09-12 실측 없이 진행한 데이터·보고서 보강
- 코드 해설에 제조사/시장/차종/연식/동력계/국내 대조 상태/재배포 권한 필드를 추가. 미확인 정보는 미확인으로 유지. 기존 공개 해설 3건이며 새 정비 절차를 검증한 것은 아님.
- 진단 기록 화면에 현재 열린 결과의 개인정보 제외 정비 요약 미리보기, 복사, Android 공유 추가. 자유 입력 차량명·VIN·MAC·원시 응답을 복사하지 않는 허용 항목 방식.
- 배터리는 별도 측정임을 표시하고 가상 데이터·불완전 응답을 구분. 과거 저장 기록의 변환, PDF, 삭제 전후 통합 보고서는 후속 작업.

## 2026-09-12 저장 기록 비교·PDF 보고서
- 저장된 JSON을 선택해 개인정보 제외 보고서를 미리 보고 텍스트 복사/PDF 공유 가능. 파일 읽기와 PDF 생성은 IO 스레드에서 수행.
- ECU+코드별 삭제 전/후/재확인/신규 수신/상태 변화를 비교. 불완전 재조회는 소거 판정을 하지 않음.
- 신규 저장 JSON에 ECU 전용 조회·삭제 전후·검증 완료 상태 추가. 기존 형식의 누락 정보는 원시 응답에서 추측 복원하지 않음.
- PDF는 A4 한글 자동 줄바꿈·페이지 번호·행 단위 페이지 나눔 적용. 공유할 임시 PDF만 cache/workshop-reports로 제한.
- 기존 진단 JSON에는 셀 전압이 없어 배터리 그래프는 포함하지 않음. 별도 시점의 배터리 기록을 자동 결합하지 않음.

## 2026-09-12 리프 코드 해설 확장
- 오프라인 해설 6건: P33ED-00, P3180-97, P317E-97 추가. 일반 화면에도 검증 상태와 확인 순서를 표시.
- NTB26-029의 P33ED 판단은 북미 캠페인 지정 작업·시험 주행 후 조건으로 한정. P3180은 업데이트 후 소거 관련 언급만 확인.
- 접미 유형(-00/-97), 국내 적용, P317E 제조사 정의는 미확인으로 표시. 새 차량 제어 명령은 없음.
- 검증: 단위 테스트 213건 실패/오류 0, debug/release 빌드 성공.
- 근거와 남은 조사: LEAF_DTC_SOURCES.md.

## 2026-09-12 진단 결과와 코드 해설 연결
- 표준 OBD 및 ECU 전용 결과에 코드별 해설·확인 순서 버튼 추가. 전체 코드가 입력된 해설 화면으로 이동하고 뒤로 복귀 가능.
- 현재 결과 요약과 저장 기록의 텍스트/PDF 보고서에 자체 작성 해설·검증 상태·적용 범위·출처·센터 확인 항목 추가.
- 전체 코드 정확 일치만 보고서에 연결하며 접미 유형이 다른 해설은 대입하지 않음. 중복 코드 해설은 한 번만 포함하고 미등록 코드는 근거 미확보 표시.
- 해설은 보고서 생성 시 앱 버전을 명시하며, 과거 측정 시점의 제조사 판정으로 표시하지 않음.
- 별도 과제: 차량별 재발 이력 집계는 이번 범위에 포함하지 않음.

검증: 단위 테스트 216건 실패/오류 0, assembleDebug/assembleRelease 성공. Galaxy A23에 업데이트 설치. 실차 명령은 실행하지 않음.


## 2026-09-12 같은 차량 코드 수신 이력
- 진단 기록에서 같은 실제 차량의 기록을 2~30개 직접 선택하고 차량 일치를 확인한 뒤 비교.
- 차량 이름·마스킹 VIN·어댑터로 차량 일치를 추정하지 않음. 원본 파일은 수정하지 않으며 선택은 화면 내 임시 상태.
- ECU 주소+전체 코드별 서로 다른 세션의 수신 횟수와 시각·조회 단계·상태를 표시. 같은 세션의 중복 수신은 반복 진단 횟수로 세지 않음.
- 모의·배터리 전용·중복 세션·손상된 기록은 비교 거부. ECU 미확인 항목과 구형 UDS 미복원 세션은 제외/한계 건수 표시.
- 코드 미수신·실패·기록 공백을 소거 또는 정상 판정으로 사용하지 않음. 자동 차량 식별과 소거 확인 후 재발 판정은 후속 과제.

검증: 단위 테스트 221건 실패/오류 0, debug/release 빌드 성공. Galaxy A23 업데이트 후 비교 안내·비활성 초기 상태 확인, 기존 기록 3건 유지. 실제 차량 명령 및 사용자 기록의 차량 일치 확인은 수행하지 않음.


## 2026-09-12 차량별 기록 분류
- 선택한 기록에 사용자 차량 별칭을 저장하고 전체/미분류/차량별 필터 제공. 같은 차종의 여러 차량은 별도 별칭 사용.
- 분류된 차량의 기록을 목록 순서로 최대 30건 선택해 기존 코드 이력 비교로 연결. 분류 변경·해제 가능.
- 분류는 원본과 분리하여 noBackupFilesDir에 원자적으로 저장. 진단 JSON/TXT와 개인정보 제외 보고서에는 별칭을 추가하지 않음.
- 기록 내용 SHA-256으로 연결하여 같은 파일명이 다른 내용으로 바뀌면 이전 분류를 물려주지 않음. 원본 수정 시 미분류로 돌아갈 수 있음.
- 신규 진단 자동 분류는 하지 않으며 실제 차량 일치는 사용자 확인 기준. 기존 기록을 리프/니로로 임의 배정하지 않음.

검증: 단위 테스트 225건 실패/오류 0, debug/release 빌드 성공. Galaxy A23 업데이트 및 차량별 필터·입력·분류 화면 표시 확인. 실제 사용자 기록의 차량 분류는 임의로 지정하지 않았으며, 저장·재실행 유지의 휴대폰 실동작 검증은 미실행.


## 2026-09-12 차량 분류 저장 검증 보강
- 디버그 앱 식별자를 com.eunho.leafobd.qa로 분리해 휴대폰 자동 테스트가 배포 앱과 실제 기록을 덮어쓰지 않도록 함.
- QA 전용 폴더에서 분류 저장/별도 프로세스 재조회/재분류/해제/기록 내용 교체/손상 파일 및 일괄 저장 실패 시 원본 보존을 검증하는 기기 테스트 추가.
- 분류 파일 크기를 메모리로 읽기 전에 검사하도록 개선.
검증 완료: 단위 테스트 225건 실패/오류 0, debug/release/AndroidTest 빌드 성공. Galaxy A23에서 QA 앱 저장 테스트 → 강제 종료 → 별도 instrumentation 프로세스 재조회·변경·해제 테스트 각각 통과. 최종 기기 테스트 클래스 전체 2건도 통과. 기록 내용 교체 시 분류 제외, 일괄 지정 실패 시 원본 보존, 손상·과대 파일 거부 확인. 이번에 설치한 QA 패키지 2개는 제거하고 정식 APK를 업데이트. 실제 사용자 기록에 분류를 임의 지정하지 않음. 이전 항목의 저장 재실행 미검증 상태는 이 검증으로 해소.

## 2026-09-12 배터리 그래프 PDF 보고서
- 배터리 화면에서 현재 열린 단일 시점 기록의 보고서 미리보기/PDF 수동 공유 제공. 저장된 기록도 다시 열어서 사용.
- SOC/SOH 용어·해석 한계, 측정 시각, 전압 수신 수/최저/최고/완전 수신 시 편차, 별도 센서/채널 온도 구분, 센터에서 확인할 측정 조건 포함.
- 전압은 64채널씩, 온도는 32항목씩 그래프 페이지와 숫자 표 제공. 누락/범위 미확인 값은 그래프 하단 × 및 표의 미확인으로 표시. 수신 범위 확대 눈금이며 정상/이상 색상 판정 없음.
- 차량명·출처 자유 입력·센서 이름 제외. 가상 예시는 모든 그래프 페이지에 표시. 다른 시각 기록·오류코드 보고서를 자동 결합하지 않음.
- Android PDF 생성은 IO 스레드에서 실행하며 기존 제한된 임시 PDF 공유 경로를 사용.
검증 완료: 단위 테스트 228건 실패/오류 0, debug/release/AndroidTest 빌드 성공. Galaxy A23 QA 앱에서 96채널(1개 누락) 및 별도 온도 3개 가상 보고서 4페이지 생성·렌더링 테스트 통과. 렌더링 PNG 4장 모두 한글/축/미수신/숫자 표/페이지 번호 시각 검수. QA 앱 제거 후 정식 APK 업데이트. 실제 차량 진단 및 외부 전송은 실행하지 않음. 검수 예시: C:/python/leafobd-build/qa/battery-report/battery.pdf (가상 데이터).
## 2026-09-12 차량 프로필과 자동 기록 연결

- 별칭·제조사·차종·연식·동력계와 선택적 EV 배터리 규격을 갖는 다중 차량 프로필 추가.
- 2019 리프 ZE1·니로 EV DE 빠른 입력과 범용 차량 직접 입력 제공. 차대번호·차량번호는 프로필에 저장하지 않음.
- 진단 전에 차량 선택을 요구하고, 차량을 바꾸면 ECU 주소 캐시와 이전 진단 화면 결과를 초기화.
- 새 표준/UDS 진단 기록은 선택한 차량 분류에 자동 연결. 새 배터리 기록은 차량 프로필 UUID를 저장하며 기존 v1 기록은 미연결 상태로 계속 읽음.
- 차량 프로필 삭제 시 기존 진단·배터리 기록은 유지. 전기차 프로필과 배터리 조회 규격이 다르면 직접 조회 차단.
- 코드 해설 화면에 현재 데이터가 자동 수집 DB가 아니며 검수된 항목만 포함한다는 상태 문구 추가.
- 검증: 단위 테스트 234건 실패/오류 0, debug/release 빌드 성공. Galaxy A23에서 프로필 2건 저장·재로드와 선택 설정 유지 기기 테스트 1건 통과. 정식 앱 업데이트 후 QA 앱 제거. 실차 명령은 실행하지 않음.
# 2026-09-12 — 진단 지식 출처·권리 원장

- 오프라인 해설 6건과 공개 근거 4건을 안정적인 출처 ID로 연결했다.
- 근거 등급, 발행처, 적용 범위, 대조일, 현재 확보한 배포 권리를 별도 원장으로 관리한다.
- 출처 ID/URL 불일치, 대조일 형식, 미검증 항목의 잘못된 원문 연결을 단위 테스트로 차단한다.
- 코드 해설 화면에 해설·근거 수, 제조사 문서 대조 수, 정의 확인 중 수, 원장 검사 상태를 표시한다.
- 표준·OEM 구독·상용 API·공공 API·오픈소스를 비교한 `DATA_ACQUISITION_RESEARCH.md`를 추가했다.
- 유료 자료의 원문이나 새 DTC 정의는 포함하지 않았고 차량 통신 명령도 변경하지 않았다.
- 검증: 단위 테스트 236건 실패/오류 0, debug/release 빌드 성공. Galaxy A23에 정식 APK 업데이트 후 실행 유지 확인.

# 2026-09-12 — 무료 공개 지식 데이터 팩

- 6개 해설과 4개 출처를 `diagnostic_knowledge_pack.json` 단일 원본으로 분리했다.
- 앱 시작 때 팩의 스키마·크기·버전·날짜·코드 형식·출처·근거 등급을 검사하고 실패한 팩은 표시하지 않는다.
- JVM 테스트도 APK와 같은 JSON을 읽도록 구성해 코드와 배포 데이터의 차이를 막았다.
- 공개 자료 제안 지침과 GitHub 이슈 양식을 추가했다. 개인정보, 유료 복제물, 유출 자료와 위험한 차량 제어 명령은 제출 대상에서 제외한다.
- 검증: 단위 테스트 238건 실패/오류 0, debug/release 빌드 성공. Galaxy A23에서 APK assets의 공개 데이터 팩 로드 기기 테스트 1건 통과.

# 2026-09-12 — 공통 OBD 해설 1차 확대

- 공개 지식 팩을 17개 해설과 8개 공식 근거 문서로 확대했다.
- P0301–P0304, P0442, P0455, P0456, P0128, P0140, P0451, P0461 해설을 추가했다.
- 공통 OBD 코드와 제조사 전용 코드를 데이터와 화면에서 구분하고 각각의 개수를 표시한다.
- 공통 코드의 뜻은 넓게 안내하되, 연결된 현대·기아 정비 공지의 사례는 원문 차종·연식·판매지역에만 적용된다고 표시한다.
- 같은 차량 적용 조건의 중복 항목과 failure type이 붙은 코드를 공통 코드로 잘못 분류하는 경우를 데이터 검사에서 차단한다.
- 검증: 단위 테스트 238건 실패/오류 0, debug/release 빌드와 release Lint 성공. Galaxy A23에서 APK assets 데이터 팩 로드 기기 테스트 1건 통과 후 정식 APK 업데이트·실행 확인. 실제 차량 명령은 실행하지 않음.

# 2026-09-13 — 서명된 오류코드 데이터 수동 업데이트

- 앱을 다시 설치하지 않고 설정에서 공개 오류코드 데이터 팩을 직접 갱신할 수 있게 했다.
- GitHub의 고정 매니페스트와 데이터 팩 주소 외에는 접속하지 않으며 요청에 차량·진단 정보를 넣지 않는다.
- 리비전·팩 버전·SHA-256·원본 바이트를 P-256 ECDSA 서명으로 묶고, 최소 앱 버전·최대 크기·JSON 스키마·출처 원장까지 검사한다.
- 새 파일을 완전히 저장한 뒤 활성 포인터를 마지막에 바꾸며, 실패·변조·중단 시 기존 다운로드 팩 또는 앱 내장 팩을 계속 사용한다.
- 공개 배포 파일 생성 도구와 개인키 백업·게시 절차를 `KNOWLEDGE_PACK_PUBLISHING.md`에 기록했다.
- 검증: 단위 테스트 242건 실패/오류 0, debug/release 빌드와 release Lint 성공. Galaxy A23에서 ECDSA 검증·저장·변조 거부·재조회 기기 테스트 2건 통과. GitHub main의 매니페스트와 팩을 다시 내려받아 SHA-256 일치 확인. 정식 APK 업데이트 설치 및 실행 확인. 잠금 화면 때문에 설정 화면 자동 시각 검수는 수행하지 못함.
